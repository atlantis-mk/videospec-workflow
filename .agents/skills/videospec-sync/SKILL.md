---
name: videospec-sync
description: Sync only reusable standards without treating a prior production's review context as automatic input.
---

# Sync durable standards

Read the production's context ledger, history, analytics, retrospective, and proposed deltas. Promote only evidence-backed reusable rules, leaving episode-specific before/after context intact in its immutable history. Run sync and report the durable changes.

For templateVersion 6, read `learning.md` instead of `analytics.md` and `retrospective.md`.
